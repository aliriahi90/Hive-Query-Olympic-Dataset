DROP VIEW IF EXISTS athlete_events;

CREATE VIEW IF NOT EXISTS athlete_events AS
           SELECT id,name,sex,age,height,weight,team,noc,games,year,season,city,sport,event,medal
           from athlete_events_m;
